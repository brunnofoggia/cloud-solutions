# user:pass demo:demo
services:
    sftp:
        image: emberstack/sftp:latest
        container_name: sftp-server
        ports:
            - '2222:22'
        volumes:
            - ./../../../tmp/sftp/data:/home/demo/sftp
        restart: unless-stopped
